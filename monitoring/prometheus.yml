global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'bianqing-backend'
    static_configs:
      - targets: ['backend:6060']
    metrics_path: /metrics
    scheme: http
